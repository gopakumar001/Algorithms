/**
 * @author Gopakumar V
 * @file Algorithm to sort content using Quick Sort.
 * -------------------------------------------
 * Steps :-
 * 
 *      1. Shuffle the array.
 *      2. Partition the array so that for some index j.
 *          - no larger entry to the left of j.
 *          - no smaller entry to the right of j.
 *      3. Sort the array recursively.
 * 
 * 
 * -------------------------------------------
 * Big(o) - 
 * Link :- 
 * 
 */

export default function quickSort() {
    return {
        sort: function (input, btmUp) {}
    };
};